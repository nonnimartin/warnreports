<table class="table table-striped responsive" id="naics_table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Code</th>
            <th>Title</th>
            <th>Reports</th>
        </tr>
    </thead>
    <tbody>
        {%- for naic in naics %}
        <tr>
            <td>{{ naic.id }}</td>
            <td>{{ naic.code }}</td>
            <td>{{ naic.title }}</td>
            <td>{{ naic.reports_count }}</td>
        </tr>
        {%- endfor %}
    </tbody>
</table>