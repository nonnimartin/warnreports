#!/bin/bash
if [ "$#" -eq 0 ] ; then
  echo "Usage: wrep <COMMAND> [...]" >&2
  exit 1
fi
set -e
export PYTHONPATH="$(realpath "$(dirname "$0")/..")"
if [[ "$@" == 'shell' ]] ; then
  exec python
else
  exec python -m wrep "$@"
fi
