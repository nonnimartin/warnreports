CREATE TABLE "contacts" (
	"email"	,
	"company"	,
	"created"	INTEGER,
	"lastNotice"	INTEGER,
	"confirmed"	INTEGER
)
CREATE TABLE companies (
	company TEXT NOT NULL,
	state TEXT NOT NULL,
	UNIQUE(company, state) ON CONFLICT REPLACE
)
