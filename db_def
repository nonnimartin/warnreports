CREATE TABLE contacts (
	email TEXT NOT NULL PRIMARY KEY,
	company TEXT NOT NULL,
	state TEXT,
	stateOnly INTGER NOT NULL,
	confirmed INTEGER,
	created INTEGER NOT NULL,
	lastNotice INTEGER
);
CREATE TABLE companies (name, state);
